{
  "name": "couch-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Internal operations tool for the couch business (inventory, trips, sales).",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "import-legacy": "node --env-file-if-exists=.env scripts/import-legacy-data.js",
    "reset-db": "node --env-file-if-exists=.env scripts/reset-db.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  }
}
